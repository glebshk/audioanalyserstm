#ifndef FFT_H
#define FFT_H

#include "arm_math.h"
#include <stdbool.h>

//#define PI 3.14159265358979

typedef enum FFT_STATUS
{
	// General
	OK = 0, NOT_INITIALIZED,

	// Initialization errors:
	INIT_FAILED

	// Other
} FFT_STATUS;

FFT_STATUS FFT_init(uint32_t uFFTSize);

void floatToQ15(const float* pSource, float* pDestination);

#endif // FFT_H
